.env
.venv/
__pycache__/
# Cached candles are large and re-fetchable; the dated constituent snapshots
# are neither -- NSE publishes no historical archive, so once a snapshot is
# lost it cannot be rebuilt. Track those.
strategy/data_cache/*
!strategy/data_cache/universe/
